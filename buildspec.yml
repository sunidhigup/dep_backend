version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean install -e -DskipTests
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/dep-backend.jar
  discard-paths: yes 
